{# templates/person/edit.html.twig #}

<!DOCTYPE html>
<html>
    <head>
        <title>Edit Person</title>
    </head>
    <body>
        <h1>Edit Person</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">Save</button>
        {{ form_end(form) }}
    </body>
</html>